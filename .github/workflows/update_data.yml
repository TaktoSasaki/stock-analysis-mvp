name: Daily Stock Data Update

on:
  schedule:
    # UTC 21:00 = JST 06:00（市場が閉まっている時間）
    - cron: '0 21 * * *'
  workflow_dispatch:  # 手動実行ボタン
  push:
    branches:
      - main
    paths:
      - 'scripts/fetch_prices.py'

permissions:
  contents: write
  issues: write       # エラー時にIssueを立てる権限

jobs:
  update-data:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Fetch stock data
        id: fetch
        run: |
          python scripts/fetch_prices.py
          echo "status=$?" >> $GITHUB_OUTPUT
        continue-on-error: true

      - name: Check for changes
        id: check_changes
        run: |
          git diff --quiet data/stock_data.json && echo "changed=false" >> $GITHUB_OUTPUT || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Commit and push if changed
        if: steps.check_changes.outputs.changed == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/stock_data.json
          git commit -m "🤖 Update stock data - $(date +'%Y-%m-%d %H:%M')"
          git push

      - name: Create issue on failure
        if: steps.fetch.outputs.status != '0'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: '⚠️ 株価データ取得エラー',
              body: `データ取得処理が失敗しました。\n\n実行日時: ${new Date().toLocaleString('ja-JP')}\n\nログを確認してください: ${context.payload.repository.html_url}/actions/runs/${context.runId}`,
              labels: ['automated', 'error']
            })

      - name: Summary
        run: |
          echo "### 実行結果 📊" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ -f data/stock_data.json ]; then
            echo "✅ データファイル生成成功" >> $GITHUB_STEP_SUMMARY
            echo "📦 ファイルサイズ: $(du -h data/stock_data.json | cut -f1)" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ データファイル生成失敗" >> $GITHUB_STEP_SUMMARY
          fi